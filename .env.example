# ── FCC CMS PWA Runtime Config ─────────────────────────────────
# Copy to `.env` for local development; production values are baked
# at build time via VITE_* args in the infrastructure compose stack.

# Base URL for the CMS admin API (must end with `/api/v1`).
VITE_API_URL=http://localhost:4000/api/v1
